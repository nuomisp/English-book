# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
REMINDER_TOKEN=REPLACE_WITH_A_RANDOM_SECRET_OF_AT_LEAST_32_CHARACTERS
DATABASE_PATH=/data/reminders.sqlite3
LISTEN_HOST=0.0.0.0
PORT=8787

# Optional. Leave ALL THREE blank for local dynamic template reminders.
# Include /v1 if your provider's base URL requires it. HTTPS only.
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
